import HomePage from "./Components/HomePage";
import { Routes } from "react-router-dom"; // The Router lives at the root of the project so that all components that you create can be accessed

import "./App.css";

function App() {
  return (
    <div className="App">
      <div>This is David teaching</div>
    </div>
  );
}

export default App;
